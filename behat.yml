default:
    formatters:
        progress: true

    suites:
        array:
            paths:    [ %paths.base%/features/common, %paths.base%/features/operators, %paths.base%/features/array ]
            contexts: [ ArrayContext ]
        doctrine_orm:
            paths:    [ %paths.base%/features/common, %paths.base%/features/operators, %paths.base%/features/doctrine_orm ]
            contexts: [ DoctrineOrmContext ]
        elasticsearch:
            paths:    [ %paths.base%/features/common, %paths.base%/features/elasticsearch ]
            contexts: [ ElasticsearchContext ]
        elastica:
            paths:    [ %paths.base%/features/common ]
            contexts: [ ElasticaContext ]
