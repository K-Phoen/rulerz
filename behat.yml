default:
    formatters:
        progress: true

    autoload:
        '': '%paths.base%/tests/features/bootstrap'

    suites:
        array:
            paths:    [ %paths.base%/tests/features/common, %paths.base%/features/operators, %paths.base%/features/array ]
            contexts: [ ArrayContext ]
        doctrine_orm:
            paths:    [ %paths.base%/tests/features/common, %paths.base%/features/operators, %paths.base%/features/doctrine_orm ]
            contexts: [ DoctrineOrmContext ]
        elasticsearch:
            paths:    [ %paths.base%/tests/features/common, %paths.base%/features/elasticsearch ]
            contexts: [ ElasticsearchContext ]
        elastica:
            paths:    [ %paths.base%/tests/features/common ]
            contexts: [ ElasticaContext ]
        eloquent:
            paths:    [ %paths.base%/tests/features/common ]
            contexts: [ EloquentContext ]
